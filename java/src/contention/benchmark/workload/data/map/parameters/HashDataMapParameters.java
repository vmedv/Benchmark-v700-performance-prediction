package contention.benchmark.workload.data.map.parameters;

import contention.benchmark.workload.data.map.abstractions.DataMapParameters;

public class HashDataMapParameters implements DataMapParameters {
    @Override
    public StringBuilder toStringBuilder() {
        return null;
    }
}
